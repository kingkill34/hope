<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duowan.hope.test.dao.UserDao">
	<select id="getUserTest" resultType="user">
		SELECT game_id as gameId,id as
		id,login_name as loginName,login_password as
		loginPassword,nick_name as nickName,type as type
		FROM user where login_name
		= #{param1}
	</select>
</mapper>